#include "operations.h"
/**
 *sub- subtracts 1 integer from another
 *@a: first parameter
 *@b: second parameter
 *Return: the subtraction of the two integers
 */
int sub(int a, int b)
{
return (a - b);
}
