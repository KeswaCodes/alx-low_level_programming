#include "operations.h"
/**
 *mod- divides 1 integer from another, returning remainder
 *@a: first parameter
 *@b: second parameter
 *Return: the remainder of the two integers
 */
int mod(int a, int b)
{
return (a % b);
}
