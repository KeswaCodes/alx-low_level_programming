#include "operations.h"
/**
 *div- subtracts 1 integer from another
 *@a: first parameter
 *@b: second parameter
 *Return: the division of the two integers
 */
int div(int a, int b)
{
return (a / b);
}
