#include "operations.h"
/**
 *mul- subtracts 1 integer from another
 *@a: first parameter
 *@b: second parameter
 *Return: the multiplication of the two integers
 */
int mul(int a, int b)
{
return (a * b);
}
