#include "main.h"

/**
 *mul- multiplies two integers
 *@a: first integer
 *@b: second integer
 *Return: result
 */

int mul(int a, int b)
{


int result;

if (a != 0 && b != 0)
result = a * b;

else
result = a * b;




return (result);
}
